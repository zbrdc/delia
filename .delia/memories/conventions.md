# Project Conventions

Coding conventions detected for this project.

## Language & Stack
- **Primary Language**: Python
- **Frameworks**: fastapi, fastmcp

## Python Conventions

### Type Hints
- Type hints are **required** on all function signatures
- Use `typing` module for complex types

### Async
- Use `async/await` for I/O operations
- Prefer `httpx` over `requests` for async HTTP

### Testing
- Use `pytest` for all tests
- Test files: `test_*.py` or `*_test.py`
- Use fixtures for common setup

### Formatting
- Code must pass linting before commit
- Use `ruff` for linting and formatting

### Logging
- Use `structlog` for structured logging
- Include context: `log.info('event_name', key=value)`

## General
- Keep functions focused and small (<50 lines)
- Document public APIs with docstrings/comments
- Handle errors explicitly, avoid silent failures

---
*Auto-generated by Delia Framework. Update as needed.*